import React from 'react';
import BlogSection from '../features/blog/components/BlogSection'; // ✅ sửa đường dẫn phù hợp với bạn

const Blog = () => {
  return (
    <div>
      <BlogSection />
    </div>
  );
};

export default Blog;
